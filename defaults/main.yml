---
#Default user for asterisk install.
asterisk_user: asteriskbpx
asterisk_pass: #no password. User can only be used from the system.

#Paths for source and cache
data_dir:     /data/new
source_dir:   "{{data_dir}}/source"
cache_dir:    "{{data_dir}}/cache"

